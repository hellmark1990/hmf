{% extends "base.html.twig" %}

{% trans_default_domain 'FOSUserBundle' %}

{% block body %}
    <div class="center">
        <h2>{% trans from "profile" %}Authorization{% endtrans %}</h2>
    </div>

    {% if error %}
        <div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>
    {% endif %}

    <form id="main-contact-form" class="contact-form" name="contact-form" method="post"
          action="{{ path("fos_user_security_check") }}">
        <div class="col-sm-5 col-sm-offset-1">

            <div class="form-group">
                <label>Name *</label>
                <input type="text" name="_username" class="form-control" required="required"
                       value="{{ last_username }}">
            </div>

            <div class="form-group">
                <label>Password *</label>
                <input type="password" name="_password" class="form-control">
            </div>

            <div class="form-group">
                <label for="remember_me">{{ 'security.login.remember_me'|trans }}</label>
                <input type="checkbox" id="remember_me" name="_remember_me" value="on"/>
            </div>

            <div class="form-group">
                <button type="submit" name="_submit" class="btn btn-primary btn-lg" required="required">Увійти
                </button>
            </div>

            <input type="hidden" name="_csrf_token" value="{{ csrf_token }}"/>
        </div>
    </form>
{% endblock content %}
